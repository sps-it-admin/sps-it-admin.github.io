**SectionLabel** — the Cinzel small-caps eyebrow above a heading; opens nearly every section on the site.

```jsx
<SectionLabel>Our Foundation</SectionLabel>
<SectionLabel tone="gold">What We Teach</SectionLabel>
```

Use `crimson` (default) on cream grounds, `gold` on navy grounds. Keep to 2–4 words.
